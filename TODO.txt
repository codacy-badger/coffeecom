TODO:

* Add support for multiple clients in one lobby

* Add a protocol-class for the server (the class should be handling input, commands etc.)

* Create a CONFIG-file (to store username, settings, lobby-code etc.)

* Make an option for users to disable colored text (store setting in CONFIG)
 
